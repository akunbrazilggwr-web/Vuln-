const labs=["xss","sqli","auth","headers","log","network"];function update(){const done=labs.filter(x=>localStorage.getItem("lab-"+x)==="done").length;const el=document.getElementById("progressText");if(el)el.textContent=`${done}/${labs.length} selesai`;document.querySelectorAll(".lab-card").forEach(c=>{if(localStorage.getItem("lab-"+c.dataset.lab)==="done")c.classList.add("done")})}update();